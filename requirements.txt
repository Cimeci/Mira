# Le squelette tourne sur la stdlib seule (asyncio, hashlib, dataclasses).
# Décommente au fur et à mesure que tu remplaces les mocks par le vrai :

# --- Stage 1 — Locator RÉEL « computer use » (mira/locator_cu.py) ---
# Activé par MIRA_LOCATOR_REAL=1 (hors MIRA_DEMO_MODE). Absent -> mock stdlib inchangé.
# ⚠️ IMPACTE L4 (image container Cloud Run / sandbox Browserbase) : deps lourdes.
# google-genai           # cerveau : Gemini 2.5 Computer Use (Vertex AI ou AI Studio)
# playwright             # bras : Chromium (puis: playwright install chromium)
# httpx                  # Stage 2/3 — appels API async (Sightengine, RDAP)
# imagehash              # Stage 2 — perceptual hash (pHash)
# Pillow                 # Stage 2 — décodage image pour le hash
# cryptography           # preuve chiffrée au repos
# python-dotenv          # chargement .env.local
# resend                 # Stage 3 — dispatch email (primaire)
# sendgrid               # Stage 3 — dispatch email (fallback)

# --- dev ---
ruff
pytest

# --- L2 API Scaffold ---
fastapi
uvicorn
sse-starlette
pydantic

# --- L2 Stretch (Analyzer/Notifier) ---
imagehash
Pillow
cryptography
httpx
