node_modules/
.next/
dist/
build/
.vercel/
.env
.env.*
!.env.example
*.log
.DS_Store
scratch/
.venv/
__pycache__/
.claude/settings.local.json
.mira_consent/

# Captures runtime du locator (générées à chaque scan, jamais versionnées)
mira/web/static/shots/

# Profil de navigateur persistant : contient les COOKIES DE SESSION (secrets !)
.mira_browser_profile/

# Plan de travail perso (non commité — cf. en-tête du fichier)
PLAN.local.md

# Artefacts de dev (logs dev.sh, cache incrémental TS)
.dev-logs/
*.tsbuildinfo

# Local runtime env (secrets) — never committed
dev.env

# Face test fixtures: real photos, not redistributable
tests/fixtures/faces/*.jpg
tests/fixtures/faces/*.png

# Nudity test fixtures (sensitive, not redistributable)
tests/fixtures/nudity/*.jpg
tests/fixtures/nudity/*.png
