# Copie ce fichier en frontend/.env.local (gitignored — voir .gitignore racine).
# Mêmes valeurs que SUPABASE_URL / SUPABASE_ANON_KEY du .env.local racine.
# NEXT_PUBLIC_* = exposé au navigateur : l'anon key est faite pour ça (RLS côté DB).
# Ne JAMAIS mettre la SERVICE_ROLE_KEY ici.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
