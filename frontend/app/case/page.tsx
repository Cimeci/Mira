import { CaseStatusView } from "@/components/case/CaseStatusView";

/** End of the case-creation flow. Renders the shared status view with defaults. */
export default function CaseCreatedScreen() {
  return <CaseStatusView />;
}
